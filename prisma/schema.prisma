datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Feature {
  id          String   @id @default(uuid())
  title       String
  description String
  icon        String?
  createdAt   DateTime @default(now())
}
